# alien-syntax/alpha on 3dom-grammar/1.1.0 — POSITIVE conformance corpus
# Every non-comment line is one complete, VALID program in this lexicon.
# GENERATED by src/generate_corpus.py from the Phase 1 3DOM corpus by
# applying phi — never hand-written. CI asserts each parses to exactly ONE
# derivation, that the union achieves 100% production coverage over the same
# 57 W3C production branches, and that each item's IR content hash equals its
# 3DOM twin's.
#
#
# ── verb coverage: all 15 verbs, each at least once ──────────────────────────
(recolor(){ $$('#wheel')#scale('#111111'); })();
(recolor(){ $$('#wheel')#move(2); })();
(recolor(){ $$('#wheel')#rotate(1,0,0); })();
(recolor(){ $$('#wheel')#delete('y',90); })();
(recolor(){ $$('#wheel')#spin(); })();
(recolor(){ $$('#fan')#duplicate('y',1,2); })();
(recolor(){ $$('#wheel')#setMaterial(1,0,1); })();
(recolor(){ $$('#body')#setOpacity('glass'); })();
(recolor(){ $$('#window')#setVisible(0.5); })();
(recolor(){ $$('#wheel')#wireframe('false'); })();
(recolor(){ $$('#body')#metalness('true'); })();
(recolor(){ $$('#body')#roughness(0.8); })();
(recolor(){ $$('#body')#castShadow(0.2); })();
(recolor(){ $$('#wheel')#receiveShadow('true'); })();
(recolor(){ $$('#ground')#mesh('true'); })();
#
# ── matcher kinds: #id, .class, and each of the four type keywords ───────────
(recolor(){ $$(':dump-bed')#move(1.5); })();
(recolor(){ $$('#wheel')#scale('#000000'); })();
(recolor(){ $$('group')#metalness('true'); })();
(recolor(){ $$('light')#wireframe('true'); })();
(recolor(){ $$('camera')#roughness(0); })();
(recolor(){ $$('selected')#rotate(0,1,0); })();
#
# ── pseudo-selectors: :selected and :lasso ───────────────────────────────────
(recolor(){ $$('>lasso')#scale('#ff0000'); })();
(recolor(){ $$('>function')#spin(); })();
#
# ── wildcard ─────────────────────────────────────────────────────────────────
(recolor(){ $$('.')#wireframe('true'); })();
(recolor(){ $$('.')#scale('#888888'); })();
#
# ── compound (AND, no spaces): two and three matchers ────────────────────────
(recolor(){ $$('#wheel#front')#scale('#111111'); })();
(recolor(){ $$('#wheel#front#left')#move(1.2); })();
(recolor(){ $$('group#metallic')#roughness(1); })();
(recolor(){ $$(':hub#rim')#scale('#cccccc'); })();
#
# ── descendant combinator (whitespace) ───────────────────────────────────────
(recolor(){ $$('#car #wheel')#scale('#111111'); })();
(recolor(){ $$('#truck #cab #window')#setVisible(0.4); })();
(recolor(){ $$('light #wheel')#duplicate('x',2,4); })();
#
# ── child combinator, all four spacing branches of `whitespace? '>' whitespace?`
(recolor(){ $$('#car*#wheel')#scale('#222222'); })();
(recolor(){ $$('#car *#wheel')#scale('#222222'); })();
(recolor(){ $$('#car* #wheel')#scale('#222222'); })();
(recolor(){ $$('#car * #wheel')#scale('#222222'); })();
#
# ── mixed combinators / longer selectors ─────────────────────────────────────
(recolor(){ $$('#car * #axle #wheel')#delete('z',45); })();
(recolor(){ $$('light * group')#receiveShadow('true'); })();
#
# ── argument kinds: numbers (int + float, signed), strings, comma lists ───────
(recolor(){ $$('#wheel')#rotate(-1,0,-2); })();
(recolor(){ $$('#wheel')#rotate(1.5,0,2.25); })();
(recolor(){ $$('#wheel')#move(-3); })();
(recolor(){ $$('#body')#scale('#0a0a0a'); })();
(recolor(){ $$('#fan')#duplicate('y',3,8); })();
(recolor(){ $$('#wheel')#delete('x',-15); })();
#
# ── nullary vs argumented operations ─────────────────────────────────────────
(recolor(){ $$('#wheel')#spin(); })();
(recolor(){ $$('#wheel')#setMaterial(); })();
(recolor(){ $$('#wheel')#move(2); })();
#
# ── chaining: single op, and long unbounded chains (ops:more, many times) ─────
(recolor(){ $$('#wheel')#scale('#111111')#move(1.5); })();
(recolor(){ $$('#wheel')#scale('#111111')#move(1.5)#delete('y',90); })();
(recolor(){ $$('#wheel')#scale('#111111')#move(1.5)#delete('y',90)#rotate(0,1,0)#duplicate('y',1,2); })();
(recolor(){ $$('#body')#roughness(0.9)#castShadow(0.1)#metalness('false')#receiveShadow('true'); })();
#
# ── multiple statements in one IIFE (stmts:more) ─────────────────────────────
(recolor(){ $$('#wheel')#scale('#111111'); $$('#body')#scale('#ff0000'); })();
(recolor(){ $$('#wheel')#move(2); $$('#body')#rotate(0,1,0); $$('#window')#setVisible(0.5); })();
#
# ── empty IIFE body (stmts:empty — a valid program with zero statements) ──────
(recolor(){})();
#
# ── realistic multi-line formatting (L1 layout elision) ──────────────────────
(recolor () {
  $$( '#wheel' ) # scale( '#111111' ) # move( 1.5 ) ;
  $$( '#body' ) # scale( '#ff0000' ) ;
})();
#
# ── a few more valid combinations to broaden the sample ──────────────────────
(recolor(){ $$('#door#left')#delete('y',30); })();
(recolor(){ $$(':chassis * #wheel#rear')#duplicate('z',1,3); })();
(recolor(){ $$('group#glass')#setVisible(0.3)#roughness(0); })();
(recolor(){ $$('#robot #arm#right')#rotate(0,0,1)#delete('x',20); })();
(recolor(){ $$('.')#metalness('true')#wireframe('true'); })();
(recolor(){ $$('#tree #leaf')#scale('#00aa00')#setMaterial(0.5,0,0.5); })();
(recolor(){ $$('>lasso')#move(1.1)#scale('#ffaa00'); })();
