# alien-syntax/alpha on 3dom-grammar/1.1.0 — VALID-BUT-VACUOUS corpus (D5)
# Every non-comment line is a SYNTACTICALLY VALID program whose chain has ZERO
# operations (a pure query): a PARSE SUCCESS and a TASK FAILURE on any task
# whose target IR has >= 1 operation (SCORING_POLICY.md).
#
# op list). Per SCORING_POLICY.md these are a PARSE SUCCESS but a TASK FAILURE on
# any task whose target IR has >=1 operation. They are neither positive (they add
#
(recolor(){ $$('#wheel'); })();
(recolor(){ $$(':dump-bed'); })();
(recolor(){ $$('#car #wheel'); })();
(recolor(){ $$('#car * #wheel'); })();
(recolor(){ $$('#wheel#front'); })();
(recolor(){ $$('.'); })();
(recolor(){ $$('group'); })();
(recolor(){ $$('light'); })();
(recolor(){ $$('>lasso'); })();
(recolor(){ $$('>function'); })();
(recolor(){ $$('#wheel'); $$('#body'); })();
(recolor(){ $$('#robot #arm#left'); })();
